# Tutorial para instação, no Windows 11, do Docker com WSL2, usando Debian 
Guia para instalação do Docker com WSL2, no Windows 11, usando Debian.

No Windows Terminal, executar:
```wsl --install -d Debian ```
